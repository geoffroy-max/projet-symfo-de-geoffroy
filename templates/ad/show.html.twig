{% extends 'base.html.twig' %}

{% block title %}{{ ad.title}} {% endblock %}

{% block body %}

    <div class="ad-title" style="background-image: url({{ ad.coverImage }})">
        <div class="container">
            <h1>{{ ad.title}} </h1>
            <p>{{ ad.introduction }}</p>
            <p class="h2">
                <strong>{{ ad.rooms }}chambres</strong> pour <strong>{{ ad.price }}&euro;</strong> par nuit
            </p>
            <a href="#" class="btn btn-success">reserve</a>
        </div>
    </div>
    <div class="container">
    <div class="row">
    <div class="col-md-8 mb-3">
    {{ ad.content|raw }}
        <div id="carouselExampleIndicators" class="carousel slide" data-ride="carousel">
            <ol class="carousel-indicators">
                {% for image in ad.images %}
                <li data-target="#carouselExampleIndicators" data-slide-to="{{ loop.index0 }}" {% if loop.first %}class="active{% endif %}"> </li>
             {% endfor %}
            </ol>
            <div class="carousel-inner">
                {% for image in ad.images %}
                <div class="carousel-item {% if loop.first %}active{% endif %}">
                    <img src="{{ image.url }}" class="d-block w-100" alt="avatar de jojo">
                    <div class="carousel-caption d-none d-md-block">
                        <h5>{{ ad.title }}</h5>
                        <P>{{ image.legende }}</P>
                    </div>
                </div>
                {% endfor %}

            </div>
            <a class="carousel-control-prev" href="#carouselExampleIndicators" role="button" data-slide="prev">
                <span class="carousel-control-prev-icon" aria-hidden="true"></span>
                <span class="sr-only">Previous</span>
            </a>
            <a class="carousel-control-next" href="#carouselExampleIndicators" role="button" data-slide="next">
                <span class="carousel-control-next-icon" aria-hidden="true"></span>
                <span class="sr-only">Next</span>
            </a>
        </div>
        </div>
        <div class="col">
            <div class="row">
                <div class="col-md-4">

                    <img src="https://via.placeholder.com/64x64" alt="avatar de jojo">

                </div>
                <div class="col">
                    <h3>kylian ndongo</h3>
                    <span class="badge badge-primary">3annonces</span>
                </div>
                <p>Lorem ipsum dolor sit amet, consectetur adipisicing elit. Animi, blanditiis cupiditate dolorem illum
                    magnam magni nihil nulla pariatur sit voluptatum.</p>
                <p>Asperiores dolor iusto magni necessitatibus quam rem soluta vel veniam! Atque distinctio doloribus eaque
                    hic officiis reiciendis saepe sed vitae?</p>
                <p>Dicta doloribus nobis non perspiciatis quasi ratione vel veritatis! Dicta eligendi enim fugit ipsa ipsum
                    quod repudiandae rerum, suscipit voluptatum.</p>
            </div>
        </div>

    </div>


{% endblock %}