{% extends 'admin/base.html.twig' %}

{% block title %} permet de modifier la reservation n° {{ booking1.id}} {% endblock %}

{% block body %}
 <div class="container-fluid">
  <h1 class="my-5"> Modification de la reservation n° {{ booking1.id }}</h1>
  {{ form_start(form) }}
<div class="row">
 <div class="col-4">
  {{ form_row(form.startDate) }}
  {{ form_row(form.endDate) }}
  {{ form_row(form.comment) }}

 </div>
 <div class="col-8">
  {{ form_row(form.booker) }}
  {{ form_row(form.ad) }}
  <button type="submit" class="btn btn-success"> <i class="fas fa-check">enregistrer</i></button>
 </div>
 {{ form_end(form) }}
</div>

 </div>







{% endblock %}