{% extends 'base.html.twig' %}

{% block title %} modification du form{% endblock %}
{% block body %}
<div class="container">
    <h1>modifier</h1>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button type="submit" class="btn btn-primary">enregistrer les modifications</button>
    {{ form_end(form) }}
</div>

{% endblock %}